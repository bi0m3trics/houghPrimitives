% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rgl_utils.R
\name{create_rgl_figure}
\alias{create_rgl_figure}
\title{Complete Workflow: Create rgl Figure with Static and Interactive Output}
\usage{
create_rgl_figure(
  scene_function,
  filename,
  width = 800,
  height = 600,
  interactive = FALSE,
  ...
)
}
\arguments{
\item{scene_function}{Function. A function that creates the rgl scene.
Should call rgl functions (spheres3d, plot3d, etc.) but NOT open3d().}

\item{filename}{Character. Base filename for PNG snapshot (e.g., "my-figure.png")}

\item{width}{Integer. Image/widget width (default: 800)}

\item{height}{Integer. Image/widget height (default: 600)}

\item{interactive}{Logical. If TRUE, return rglwidget for HTML output (default: FALSE)}

\item{...}{Additional arguments passed to scene_function}
}
\value{
If interactive=TRUE, returns rglwidget. Otherwise, returns path to PNG invisibly.
}
\description{
High-level function that creates an rgl scene, saves a static PNG, and optionally
returns an interactive widget. Designed for use in vignette/article chunks.
}
\details{
This function automates the complete workflow:
\enumerate{
  \item Opens new rgl device
  \item Executes scene_function to create visualization
  \item Saves static PNG snapshot
  \item Optionally creates interactive widget
  \item Closes rgl device
}
}
\examples{
\dontrun{
# Define scene
my_scene <- function(n_points = 100) {
  points3d(rnorm(n_points), rnorm(n_points), rnorm(n_points), col = "blue")
  axes3d()
  title3d("Random Points", "x", "y", "z")
}

# Static PNG only
create_rgl_figure(my_scene, "random-points.png", n_points = 50)

# Interactive widget (HTML output)
create_rgl_figure(my_scene, "random-points.png", interactive = TRUE)
}

}
